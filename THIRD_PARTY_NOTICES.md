# Third-party notices

ActionStream evaluates the
[`lerobot/xvla-libero`](https://huggingface.co/lerobot/xvla-libero)
checkpoint through [LeRobot](https://github.com/huggingface/lerobot) in
[LIBERO](https://github.com/Lifelong-Robot-Learning/LIBERO).

The short task-context clips included with the v1.0.0 demonstration were
recorded from the official synchronous baseline. They are illustrative task
footage, not recordings of the M4 `sync_hold`, `async_naive`, or
`async_aligned` conditions. The demonstration's M4 charts and queue telemetry
come from the checked-in frozen evidence.

The v1.1.0 demonstration uses same-reset frozen X-VLA comparison footage and
separately labeled native Isaac development footage. Its LIBERO simulation
assets, LeRobot runtime, policy checkpoint, and native simulator assets retain
their upstream terms. The curated MP4 is evidence of the recorded benchmark;
it does not relicense those upstream assets.

This notice records provenance and does not replace or extend the licenses of
the upstream projects, checkpoint, simulator assets, or datasets. ActionStream
does not redistribute the X-VLA checkpoint weights or LIBERO datasets.
