# 具身 Agent blocker 定位与修复 — 2026-09-15

## 结论

现有证据支持继续完成 ActionStream 的有限具身 Agent 集成。可复现的软件缺陷可以直接修复；语言与视觉能力必须用独立结果验收。使用更强的开发助手不等于部署的 Qwen3-VL-2B/X-VLA 自动获得新能力。

本轮完成代码审查、旧证据 CPU 回放、图像检查、评分器修复和回归测试。新增模型调用、GPU 训练、native episode 和恢复实验均为 0。没有重写旧冻结协议或旧结果。

审查起点为本地 `c683cfb2301f982cf1c5e2df9980e1c594ad0084`。在独立副本的 `codex/actionstream/visual-score-integrity` 分支工作；原工作区和 DISPATCH_005 快照保留。原 PR #3 的语言/视觉质量 FAIL 保持成立。

## 第一轮：确认失败机制

### 1. 语言遗漏：原文引用不等于完整语义

v2 检查目标、目的地引用是否出现在原文，却不保证其余动作、对象和约束均获处理。已有 v3 采用整句有限语法和宿主授权，未消费义务触发拒绝或 unknown。

本轮对 DISPATCH_004 已消费的 v2 原始输出重新运行 v3，不重新调用模型：

| 旧数据组 | v2 正项接受 | v3 正项接受 | v2 负项误放行 | v3 负项误放行 |
|---|---:|---:|---:|---:|
| 当时的 48 条开发集 | 24/24 | 22/24 | 1/24 | 0/24 |
| 24 条回归集 | 12/12 | 12/12 | 1/12 | 0/12 |

两个新增 unknown 分别含 `With care` 和 `from the counter`。它们在旧标注中是支持项，但新有限合同不能验证其约束。必须保留这一召回代价，不把已消费数据回放称为新模型能力通过。v3 是已有实现，本轮贡献是重新核验其效果和边界。

### 2. 视觉方法：单视角参考图用于两个视角

`prepare_protocol.py` 从校准 episode 的外部相机裁取目标侧面和篮子参考图，以同一图像的 2 像素扰动及干扰区域设阈值。运行时 `visual_evidence.verify` 却对两台相机使用相同参考图、相同阈值，并要求两路证据都满足完成条件。

本轮重新从原始 PNG 和已有分析员框计算 HSV 直方图相似度：

| 目标区域 | 校准 episode | 其余三条已消费 episode |
|---|---:|---:|
| 外部相机 | 5/5 通过 | 14/15 通过 |
| 腕部相机 | 0/5 通过 | 0/15 通过 |

冻结目标阈值为 0.8686023294593851；腕部评估区域得分最大仅 0.776526122407004。实际图像显示，外部相机能见彩色侧标签，腕部视角多见灰色罐盖或严重裁切的近景。这是视角相关的外观失配。即使模型提出这些分析员目标框，也仍会被拒绝；这不是对所有可能框的数学上界，也不是模型定位性能测量。

因此不能只改 JSON、盲目重跑旧试验、降低同一已消费集的阈值，或改成任意一个相机声称 inside 即完成。2D 框重叠也不是物理 3D 包含证明。

### 3. 新发现的软件缺陷：评分器信任 worker 的最终判断

原 `research_scoring.score_vision` 直接读取 `receipt.verified`，只按调用数、条目数与正负样本指标决定质量门槛，未从原始输入和输出重建判断。

复现：构造恰好 84 个已失败模型调用，同时在 worker 的 verified 字段填入合成正确结果，旧评分器返回 PASS。此记录是故障注入测试，不表示历史试验发生了篡改；旧 DISPATCH_005 实际模型调用仍为 0。

## 第二轮：修复与反证检查

新增 `visual_replay.py`，在评分前执行 CPU 独立复算：

- 核验完整调用集合、原始图像文件 hash、原始/翻转/遮挡图片绑定、参考图、prompt 和逐图 tensor hash 的记录完整性。
- 核验模型调用完成状态及输出 schema。
- 从原始输出与图片重做外观、区域、反事实和时间一致性检查。
- 将重建结果与 worker 记录比较；缺失输入、错误调用、被修改的记录均不能形成质量 PASS。
- 评分使用重建结果，保留 all-unknown 质量失败。

这验证的是软件记录一致性，不是密码学可信执行证明；张量 hash 仍属于被记录的模型运行凭据，CPU fixture 不能证明任何视觉模型能力。

验证：新增 10 项完整性测试，覆盖有效记录、缺失复算输入、全部调用失败、伪造判断、改写 raw、RGB/hash/prompt 缺失或不符、调用集合错误及源图片变化。连同相关语言/视觉合同共 **48/48** 通过；Ruff 与 diff whitespace 检查通过。完整有效的合成记录仍可通过，说明修复没有通过一律拒绝来规避问题。相同异常诊断记录修复后返回 FAIL。

复现测试：

```text
python -m pytest tests/test_visual_score_integrity.py tests/test_instruction_visual_evidence.py tests/test_grounded_language.py tests/test_llm_vla.py -p no:cacheprovider
```

本地保留 `diagnose.py`、`diagnosis_before.json`、`diagnosis_after.json` 和 `cpu_visual_integrity.xml`；小型摘要位于 [historical summary](https://github.com/Yanagisawa2002/ActionStream/blob/ed05167541fc9d83c93c1f9f1b422bcb05fee54f/reports/blocker_diagnosis_20260915/summary.json)。旧 raw 与冻结数据保持原位。

## 收敛：下一步最小实验与可主张范围

| 优先级 | 工作 | 验收与允许的结论 | 计算范围 |
|---|---|---|---|
| P0 已完成 | 修复评分器完整性 | 可说评分器阻断已复现的错误验收路径；不能说视觉已改善 | CPU；本轮 48 项测试 |
| P1 | 对完整指令授权做独立挑战 | 报告支持召回与负项误放行；通过有限开发集仍不代表开放语言理解 | 复用固定 2B 模型；旧方案上限 120 次文本调用，不必耗尽 |
| P1 | 先做分相机、分可见部位的校准可行性诊断 | 每个必需视角都有可分辨的正例与干扰例；罐盖无法识别身份时保留 unknown，并明确需要的时间跟踪/身份来源 | CPU 先行；有可行校准后才冻结新 GPU 协议 |
| P2 | 用新 episode 的图像验视觉方法 | 按 episode 隔离开发与评估；同时要求零错误完成、足够正例召回和有效覆盖，禁止以全部 unknown 通过 | 复用固定权重；旧预算参考为累计 1800 秒，实际成本待测，新数据另记 |
| P3 | 进入有限语言—执行—观察—恢复闭环 | 只有组件独立通过后运行；比较固定预算下成功率、误判、恢复与调用代价 | 尚未启动，工期与 GPU 成本 unavailable |

不建议把 LangMani 0/30 的自定义训练作为主线当前前置条件。其相对动作实验已降低离线误差，却仍为训练 reset 1/30、验证 0/30；这不支持“换动作表示即可修复”。已经工作的 X-VLA/LIBERO 原生基线可以承载 Agent 集成。

## 资源与运行状态

本轮两次 RTX 4090 快照利用率为 48% 和 45%，空闲显存约 22 GiB；未满足旧冻结协议的利用率不高于 10% 要求。WDDM 进程报告含图形应用、权限不足与 N/A 项，不能据此准确分离训练负载或保证空闲。未启动 GPU 等待器，未改变阈值，未中断任何现有进程。

上次 Astra 容量错误是历史任务执行中断，不是算法无解的证据。本轮没有重新测试模型服务容量。

## 审查方法与来源

本轮按 research-review 的结构在当前任务完成：第一轮提出并复现失败机制，第二轮以反例测试修复，最后形成可主张范围和最小实验清单。未委派子 agent，外部 reviewer ID 不适用。

核查本地 Qwen 调用使用 `AutoProcessor.apply_chat_template(..., tokenize=True, return_dict=True, return_tensors='pt')` 并将模型输入传给 `generate`，与[官方模型调用示例](https://huggingface.co/Qwen/Qwen3-VL-2B-Instruct)结构一致。此核对不排除图像方向、目标尺度、遮挡、校准或模型空间判断能力的问题，也没有用官方 benchmark 代替本项目结果。
