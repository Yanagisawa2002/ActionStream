# ActionStream

ActionStream tests whether compensating for observation age during asynchronous
action-chunk execution preserves success under real X-VLA inference latency and
configurable delivery delay.

The benchmark is being built gate-by-gate. Commands and measured results will be
filled in only after the corresponding stage has run successfully.
