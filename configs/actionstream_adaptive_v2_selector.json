{
  "schema_version": 2,
  "selector_id": "actionstream_adaptive_v2",
  "frozen_utc": "2026-08-17T11:20:00Z",
  "development_parent": {
    "selector_id": "actionstream_adaptive_v1",
    "selector_sha256": "e5cd59bbe760a5d7c9b6e4b1addbb039af31f4962d622af8973944e2125006d5",
    "canary_finding": "A global object-suite z bound rejected a spatial-suite command 7 mm from the observed robot EEF pose. Formal holdout was not opened."
  },
  "inputs": {
    "allowed": [
      "delivered_result_age_steps",
      "queue_depth_steps",
      "incoming_action_chunk",
      "request_observation_robot_eef_pose",
      "last_executed_action"
    ],
    "forbidden": [
      "delay_profile_key",
      "task_success",
      "future_observation",
      "holdout_task_label",
      "holdout_result"
    ]
  },
  "decision": {
    "fresh_full_max_age_steps": 5,
    "aligned_max_age_steps": 20,
    "hard_max_age_steps": 28,
    "minimum_aligned_horizon_steps": 10,
    "age_ewma_alpha": 0.25,
    "preferred_modes": {
      "fresh": "full_chunk",
      "moderate": "age_aligned",
      "severe_but_bounded": "full_chunk",
      "outage_or_unsafe": "safe_hold"
    }
  },
  "risk_gate": {
    "reference_mode": "request_observation_eef_then_last_action",
    "maximum_position_disagreement_m": 0.25,
    "maximum_rotation_geodesic_radians": 0.35,
    "reject_non_finite": true,
    "record_gripper_switch": true,
    "hard_gate_precedes_mode_preference": true,
    "first_chunk_without_robot_eef_reference": "abort"
  },
  "capabilities": {
    "full_chunk": true,
    "age_aligned": true,
    "safe_hold": true,
    "official_rtc_dynamic_switch": false,
    "rtc_boundary": "RTC remains an external official baseline in v2; it is not relabeled as an Adaptive branch because RTC changes policy inference as well as queue merge."
  },
  "claims": {
    "primary": "Select execution semantics from live result age and frame-local action risk across task suites without reading the delay profile or task outcome.",
    "non_claims": [
      "No claim of learned selector optimality.",
      "No general safety certification.",
      "No real-robot result until the separately frozen paired protocol is executed."
    ]
  }
}
